<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.extremeplayer.shopme">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App.AppController"
        android:allowBackup="true"
        android:icon="@drawable/shopamazon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UserActivities.ProductActivity"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".UserActivities.MainActivity"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".UserActivities.GadgetActivity" />
        <activity
            android:name=".UserActivities.SplashActivity"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserActivities.InstructionActivity" />
        <activity android:name=".UserActivities.PaymentActivity" />
        <activity android:name=".UserActivities.VoiceActivity"
            android:theme="@style/AppTheme.CustomTheme"></activity>
    </application>

</manifest>